# Desktop testing requirements for mobile P2P app
# Install with: pip install -r requirements-desktop.txt

# Core libp2p dependencies
base58>=1.0.3
coincurve>=10.0.0
grpcio>=1.41.0
lru-dict>=1.1.6
multiaddr>=0.0.9
mypy-protobuf>=3.0.0
noiseprotocol>=0.3.0
protobuf>=4.25.0,<5.0.0
pycryptodome>=3.9.2
pymultihash>=0.8.2
pynacl>=1.3.0
rpcudp>=3.0.0
trio-typing>=0.0.4
trio>=0.26.0
fastecdsa==2.3.2; sys_platform != 'win32'
zeroconf>=0.147.0,<0.148.0

# Desktop UI (Kivy alternative for testing)
kivy>=2.2.0

# QR code support
qrcode>=7.4.2
Pillow>=10.0.0

# Optional: GUI alternative for desktop testing
# tkinter is built-in to Python
# PyQt5>=5.15.0
# PySide6>=6.0.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-trio>=0.5.2
